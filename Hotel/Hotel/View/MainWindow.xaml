<Window x:Class="Hotel.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hotel.View"
        xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Menu" Height="830" Width="1250"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Panel.ZIndex="999"
        Name="MainAdminWindow"
        >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="55"/>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--BackGround-->
        <Border CornerRadius="0,20,20,0"
                Background="White"
                Grid.Column="2"
                Grid.ColumnSpan="2"
                Margin="-25,0,0,0"
                Grid.RowSpan="2"
                MouseDown="Border_MouseDown"
                MouseLeftButtonDown="Border_MouseLeftButtonDown"/>
        <Border CornerRadius="20"
                Background="#99c2ff"
                Grid.RowSpan="2"/>
        <!--LeftMenu-->
        <Border 
                Grid.Column="1"
                Grid.RowSpan="2"
                CornerRadius="20"
                Background="#99c2ff"
                Panel.ZIndex="1"
                Margin="-55,0,0,0" 
                >
            <Grid>
                <!--TopCombobox-->
                <ComboBox VerticalAlignment="Top"
                          Margin="25 45 0 0">
                </ComboBox>
                <!--Menu Buttons-->
                <StackPanel Margin="0 160 0 0">
                    <local:MenuButton Title="Phòng"
                                   Icon="HomeOutline" />

                    <local:MenuButton Title="Đặt phòng"
                                   Icon="ChartTimelineVariantShimmer" />

                    <local:MenuButton Title="Quản lý khách hàng"
                                   Icon="AccountMultipleOutline" />

                    <local:MenuButton Title="Quản lý phòng"
                                   Icon="MessageOutline" />

                    <local:MenuButton Title="Hóa đơn"
                                   Icon="CurrencyUsd" />

                    <local:MenuButton Title="Quản lý dịch vụ"
                                   Icon="MapMarkerOutline" />
                </StackPanel>
                <StackPanel VerticalAlignment="Bottom"
                            Margin="25 0 0 40">
                    <local:MenuButton Title="Logout"
                                   Icon="Logout" />
                </StackPanel>

            </Grid>
        </Border>
        <!--Top Section-->
        
    </Grid>
</Window>
